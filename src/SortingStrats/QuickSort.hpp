#ifndef QUICKSORT
#define QUICKSORT

#include <bits/stdc++.h>
using namespace std;

Class QuickSort : SortingStrategy{
    public:
        QuickSort(unsigned int size) : SortingStrategy(size){}
    private:
        void sort(arr[string]){
            //add sort code here
        }
}

#endif
